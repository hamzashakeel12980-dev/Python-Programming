profile = {
    "name": "Hamza",
    "age": 22,
    "city": "Lahore"
}
# age = profile.get("age-2", "Age not found")
# print(age)
keys = profile.keys()
print(list(keys))
values = profile.values()
print(list(values))
items = profile.items()
print(list(items))